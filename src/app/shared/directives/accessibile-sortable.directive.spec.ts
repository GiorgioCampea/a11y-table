import { AccessibileSortableDirective } from './accessibile-sortable.directive';

describe('AccessibileSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessibileSortableDirective();
    expect(directive).toBeTruthy();
  });
});
