import { EntityPropertyValuePipe } from './entity-property-value.pipe';

describe('EntityPropertyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EntityPropertyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
